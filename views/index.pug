include header.pug
html
  head
    meta(charset='utf-8')
    title Spoontastic
    link(rel="stylesheet" href="./main.css")
    style.
      button {
        margin-left: 20px
      }
    script(src='myscripts.js')
  body
    header
      
    unless !user
      h2 Enter Search Terms:
        form(autocomplete="off")
          input(type="text" name="foodSearch" id="foodSearch") 
          button(onclick=`tryAPI("pasta"))`) Search
      #results
        ul(id="resultsList")
include footer.pug

